# דוח מצב — אפליקציית "קרוב"

> מסמך חי. מתעד את כל מה שנעשה בפרויקט. מתעדכן לאורך הדרך.
> **עודכן לאחרונה:** 2026-07-08 (כל המספרים אומתו ישירות מ-`places.osm.json` החי)

---

## 1. כללי

- **שם:** קרוב — "כל מה שיהודי צריך, קרוב אליך" (שם אייקון מקוצר: "קרוב").
- **מטרה:** אפליקציית מובייל למציאת מקומות שיהודי צריך בקרבת מקום (מסעדות כשרות, בתי כנסת, מקוואות ועוד), בעברית RTL.
- **סטאק:** Expo SDK 56 · React Native · TypeScript · React Navigation · Leaflet + OpenStreetMap · גופן Heebo.
- **אילוצים:** לא Google Maps · לא שירותים בתשלום · בלי התחברות (auth) בשלב זה · דאטה חוקי/חינמי/מבוסס‑אמת · מובייל בלבד (לא אתר; הדפדפן משמש לתצוגה מקדימה בלבד).

---

## 2. מצב נוכחי (תקציר)

✅ אפליקציה מלאה ועובדת עם **דאטה אמיתי** — **5,535 מקומות** ב‑5 קטגוריות, ב‑**479 ערים**:
**4,419 בתי כנסת** (OSM 1,437 + 14 ערי ArcGIS 1,857 + 14 מועצות דתיות 1,125) · **696 מקוואות** (data.gov.il רשמי 470 + seed 202 + OSM 24) · **346 מסעדות כשרות** (OSM) · **74 בתי חב״ד** (OSM 9 + seed/manual 65).
✅ עוברת בדיקת טיפוסים (`tsc`) ומתקמפלת נקי (גם האפליקציה וגם שכבת ה‑importers).
✅ **שכבת importers מודולרית** (`importers/`) עם **3 מנועים גנריים:** parser מועצות variant-aware (SabaiApps) · adapter ArcGIS REST (עיר = config) · importer חב״ד רב‑מקורי.
✅ **חוקי ברזל נשמרים:** additive‑only · **0 מחיקות אי‑פעם** · provenance לכל רשומה · backup טרי + שער ולידציה בכל merge.

> ⚠️ צמיחה מאז 18.6: 3,402 → 5,535. עיקר התוספת — 14 ערי ArcGIS (+1,857 בתי כנסת), עוד מועצות, ו‑קטגוריית בתי חב״ד החדשה.

---

## 3. מה נבנה

### מסכים
- **בית** — ברכה לפי יום (שבת/חול), שם משתמש אישי, כרטיס CTA מוגבה "מה יש סביבי?", גריד קיצורים 3+2 (מסעדות/בתי כנסת/מקוואות + בתי חב״ד/קברי צדיקים), פרשת השבוע, מקומות קרובים. תפריט ☰ (AppMenu) + כפתור גלובוס לשפה.
- **מפה** — Leaflet + OSM עם ריכוז סמנים (clustering); לחיצה על סמן → כרטיס תחתון; כותרת משתנה לפי הקטגוריה; סיכת מקווה כחולה, סיכת בית חב״ד סגולה.
- **רשימה** — חיפוש (שם/כתובת/עיר), סינון, מיון (שם/מרחק); כותרת משתנה לפי הקטגוריה.
- **פרטי מקום** — שם, כתובת, סוג כשרות/אוכל או נוסח, טלפון, שעות, תאריך אימות, כפתורי Waze/התקשרות/דיווח, ❤️ מועדפים. למקווה במיקום משוער מוצג חיווי "מיקום משוער לפי עיר".
- **דיווח על טעות** — סוג בעיה + טקסט חופשי.
- **מועדפים** — מסך ייעודי.

### ארכיטקטורה
- **שכבת דאטה נקייה:** כל המסכים ניגשים דרך `data/placesRepository.ts` בלבד (אף מסך לא מייבא דאטה ישירות). מתג מקור ב‑`data/config.ts`: `osm` (פעיל) / `mock` / `supabase` (stub).
- **ניווט:** React Navigation — טאבים (בית/מפה/רשימה) + stack (פרטים/דיווח/מועדפים).
- **State גלובלי:** Filters, Location (משותף בין מסכים), Favorites (נשמר מקומית ב‑AsyncStorage).
- **מפה:** הפשטה `MapView` → Leaflet ב‑WebView (native) / iframe (web). חינמי, בלי מפתח.
- **עיצוב:** theme מרכזי (צבעים/ריווח/טיפוגרפיה), כפתורים גדולים, RTL כפוי, גופן Heebo בכל הטקסטים.

---

## 4. דאטה

### בשימוש כעת (באפליקציה) — 5,535 מקומות
| קטגוריה | מקור | כיסוי | רישיון | קואורדינטות |
|---|---|---|---|---|
| בתי כנסת | OpenStreetMap (Overpass) | 1,437 | ODbL | מקוריות |
| בתי כנסת | **14 ערי ArcGIS REST** (GIS עירוני פתוח) | **1,857** | ציבורי | מקוריות (`outSR=4326`) |
| בתי כנסת | **14 מועצות דתיות** (SabaiApps Directories Pro) | **1,125** | ציבורי/ODbL | מקוריות (waze/markers) |
| מסעדות כשרות | OpenStreetMap (`diet:kosher`) | 346 | ODbL | מקוריות |
| מקוואות | **data.gov.il "מקוואות טהרה"** 470 + seed (מועצות/GovMap) 202 + OSM 24 | **696** | פתוח/ציבורי | חלקן **geocoding** (GovMap/Nominatim) |
| **בתי חב״ד** | OSM 9 + seed/manual 65 (importer רב‑מקורי) | **74** | ODbL/ציבורי/CC0 | מקוריות + GovMap |

**14 ערי ArcGIS:** ת"א 475 · אשדוד 278 · חיפה 262 · אשקלון 188 · מודיעין עילית 155 · חולון 109 · הרצליה 81 · ירושלים 79 · לוד 78 · נהריה 71 · אור עקיבא 32 · מודיעין 22 · נוף הגליל 15 · קרית אונו 12. **adapter גנרי אחד** (`importers/arcgis/`) — עיר = שורת config; `outSR=4326` (כולל הסטת‑הדאטום הישראלי ~77מ׳); native coords only.

**14 מועצות דתיות:** פתח תקווה · נתניה · גבעת שמואל · יהוד · מרחבים · ראש העין · גני תקווה · באר שבע · לוד · בית שאן · מרום גליל · קרית ארבע · חדרה · גבעת זאב. parser אחד variant-aware (waze ll / markers array), additive בלבד — קיימים מועשרים (id נשמר), חדשים מצורפים עם `provenance`, conflicts/over300 נשמרים בדוחות (לא מוזגו). 0 מחיקות.

- נבנה ע"י שכבת **`importers/`** (synagogues / religious-councils / arcgis / mikvahs / kosher-restaurants / chabad / shared). הפלט: `src/data/generated/places.osm.json` + `cities.osm.json` (**479 ערים**).
- כל מקום משויך לעיר; כתובת = רחוב כשיש, אחרת העיר. ⚠️ ל‑OSM אין תעודות כשרות רשמיות — שדות אלה ריקים, לא מומצאים.
- **מקוואות — geocoding:** המקור הרשמי מלא (שם/טלפון/שעות/נגישות ~100%) אך **בלי קואורדינטות**. נפתרו 470/606: **292 ברמת כתובת** מדויקת, **178 ברמת עיר** (מסומנות `locationPrecision:'city'` → חיווי "מיקום משוער"). 136 שלא נפתרו אמין (בעיקר יישובי יו"ש שאין ב‑Nominatim) **לא נכנסו** — אין מיקום עדיף על מיקום שגוי.
- backup לפני חיבור המקוואות: `src/data/generated/places.osm.premikveh.backup.json` (1,936).

### פער פתוח: עסקים כשרים
| קטגוריה | מצב |
|---|---|
| עסקים כשרים (תעודת כשרות אמיתית) | ⚠️ **פער פתוח** — אין מקור חינמי אמין (OSM זעום; מאגר רשמי פתוח רק ~63 ארצי/פיילוט; המאגר המלא מאחורי פורטל סגור, בלי API). |

- קבצי מחקר מקור: `research/importer-poc.mjs` + פלטים ב‑`research/out/`.

---

## 5. החלטות מפתח

- **נשארנו עם אפליקציית React Navigation הקיימת** (לא התחלנו מחדש על Expo Router).
- **מפה:** מ‑MapLibre (native בלבד, לא רץ ב‑Expo Go/דפדפן) → **Leaflet** (רץ בכל מקום). MapLibre הוסר.
- **דאטה:** נבחר **OpenStreetMap** (חוקי/חינמי) אחרי שנמצא שאין API רשמי פתוח לכשרות; מקוואות מ‑**data.gov.il** הרשמי.
- **importers מודולרי:** שכבת `importers/` (קטגוריה‑לתיקייה, סכמה אחידה, `shared/`) מחליפה את `scripts/fetch-osm-places.mjs`. ריצה ישירה של `.ts` ב‑Node (ESM, `importers/tsconfig.json` נפרד — לא משפיע על בילד Expo).
- **חיבור מקוואות מבוקר:** בוצע בשבעה שלבים (inspection → importer → geocoding → output → mapper → preview merge → חיבור), עם backup, שער ולידציה, ו‑`app tsc` אחרי כל שלב — בלי refactor.
- **מסלול מועצות דתיות:** מקור הכיסוי הגבוה ביותר. רוב המועצות על **SabaiApps Directories Pro** עם REST‑index + עמודי דירקטורי (`field_*` + קואורדינטות מקוריות). parser אחד variant-aware: מסלול **waze** (קואורד׳ בקישור Waze) ומסלול **markers** (מערך markers + העשרה מהכרטיס לפי שם). אין geocoding (קואורד׳ מקוריות). חוקי: גוף ציבורי, robots מתיר.
- **דה‑דופ/העשרה additive:** התאמה לפי קרבה (≤300מ׳) + דמיון‑שם; קיים → העשרה (מילוי שדות ריקים, id נשמר); חדש → צירוף; **לעולם לא מיזוג לפי קואורדינטות בלבד**; conflicts/over300 לדוחות, לא בכוח.
- **שם:** "קרוב – כל מה שיהודי צריך, קרוב אליך".

---

## 6. הרצה / תצוגה

```bash
cd kosher-app
npx expo start            # מובייל (Expo Go) — המפה רצה כ‑Leaflet אמיתי
npx expo start --web      # תצוגה מקדימה בדפדפן (http://localhost:8081)
```
- אין אמולטור/Mac בסביבה הנוכחית; התצוגה נעשית בדפדפן (preview בלבד).
- iOS עם מפה native אמיתית ידרוש EAS Build בעתיד.

---

## 7. פתוח / הצעדים הבאים (לא בוצעו)

- [x] **מקוואות** — ✅ בוצע: importer מ‑data.gov.il + geocoding, 470 מקוואות מחוברים לאפליקציה (2026‑06‑17).
- [x] **בתי כנסת — מועצות דתיות** — ✅ בוצע: 12 מועצות חוברו (+996), בתי הכנסת 1,590 → 2,586 (2026‑06‑18).
- [x] **באר שבע** — ✅ בוצע דרך מועצת ב"ש (SabaiApps, 165 בתי כנסת).
- [ ] **מועצות נוספות** — discovery מלא ל‑131 (יש רשימת דומיינים חלקית); משיכת עוד מועצות SabaiApps לעבר ~6,000–7,000. הרצליה דורשת variant.
- [ ] **סקירת דוחות צעד 1/0** — conflicts + over300 + נפסלות‑ב"ש (ללא כתובת) — להחלטה ידנית.
- [ ] **עסקים כשרים** — להשיג מקור אמין (בקשת המאגר המלא מהמשרד לשירותי דת / שותפות גוף כשרות / הזנה קהילתית).
- [x] **בתי חב״ד — מיזוג Phase 3** — ✅ בוצע (2026-06-23 → הורחב 2026-06-24): **74 בתי חב״ד** מחוברים ב‑44 ערים (additive, גיבוי+שער, 0 מחיקות). importer חוקי `importers/chabad/` (OSM+amutot+Wikidata+manual). מקורות פתוחים = תקרה חלקית (~8% מ‑941).
- [x] **ArcGIS REST — מקור עירוני** — ✅ בוצע: adapter גנרי, **14 ערים** (+1,857 בתי כנסת), עיר = config.
- [ ] **בתי חב״ד — הדרך ל-90%** — פנייה רשמית לצעירי אגודת חב״ד (עמותה #580021673, 072-2770100) לקבלת קובץ/רישוי. זה המקור היחיד שמגיע ל-≥90% עם קואורדינטות (chabad.org Locator חסום ב-ToS). 22 בתי חב״ד כבר קיימים כבתי כנסת (overlap, לא מוזגו).
- [ ] **מקוואות — שיפור geocoding** — 136 שלא נפתרו (יו"ש); לשקול מקור קואורדינטות נוסף או הזנה ידנית.
- [ ] **סנכרון אוטומטי** — אימפורטר מתוזמן + משיכה מרחוק לאפליקציה (נדחה; שלב מחקר בלבד כרגע).
- [ ] **Supabase** — חיבור backend אמיתי (כרגע stub).
- [ ] שיפורי כתובות/טלפונים חסרים.

---

## 8. יומן שינויים

- **2026-06-16** — הקמת הפרויקט; 5 מסכים; שכבת דאטה; mock data; MapLibre→Leaflet; RTL; שם "קרוב".
- **2026-06-16** — פיצ'רים v2: סוגי מקום (מסעדה/בית כנסת), מועדפים, כרטיס תחתון במפה, מיקום משותף, תאריך אימות.
- **2026-06-17** — פוליש עיצוב + גופן Heebo; דאטה אמיתי מ‑OSM (1,936); clustering; כותרות דינמיות; חיפוש לפי עיר; **שלב מחקר מקורות דאטה (PoC)**.
- **2026-06-17** — **שכבת `importers/` מודולרית** (synagogues / mikvahs / kosher-restaurants / shared) שמחליפה את הסקריפט החד‑פעמי; ריצת `.ts` ישירה ב‑Node.
- **2026-06-17** — **חיבור מקוואות** (data.gov.il + geocoding): 470 מקוואות נוספו → **2,406 מקומות**. `PlaceType` הורחב ל‑`mikveh`, קיצור "מקוואות" בבית, סיכה כחולה, חיווי "מיקום משוער לפי עיר" (178 city-level). backup: `places.osm.premikveh.backup.json`.
- **2026-06-18** — **מסלול מועצות דתיות** (`importers/religious-councils/`): מחקר + parser variant-aware ל‑SabaiApps Directories Pro (מסלולי waze/markers, ללא geocoding). **צעד 0:** 7 מועצות (PT/נתניה/גבעת שמואל/יהוד/מרחבים/ראש העין/גני תקווה) → +656 חדשים, 73 העשירו → **1,590 → 2,246** בתי כנסת. backup: `places.osm.precouncils.backup.json`.
- **2026-06-18** — **מועצות דתיות צעד 1:** 5 מועצות (באר שבע/לוד/בית שאן/מרום גליל/קרית ארבע) → +340 חדשים, 35 העשירו → **2,246 → 2,586** בתי כנסת (סה"כ **3,402 מקומות**). תיקון markers: כתובת מהכרטיס ב‑fallback. backup: `places.osm.pre-step1.backup.json`. additive, 0 מחיקות, tsc ירוק.
- **2026-06-22** — **בתי חב״ד — Phase 1 (מחקר):** נבדקו 4 מקורות. אין מקור ממשלתי פתוח (רשת עמותה פרטית). Chabad.org Locator = העשיר ביותר (~941, קואורדינטות, עברית) אבל ToU מגביל; chabad.org.il חוסם בוטים (403); Chabadpedia ללא כתובות/קואורדינטות. **הוחלט (אישור משתמש): OSM בלבד** (חוקי, ODbL, כבר בצינור). אומדן Overpass: ~47 אלמנטים.
- **2026-06-23** — **בתי חב״ד — מקור "manual" + שוהם:** נוסף מקור רביעי `importers/chabad/manual.json` (הזנה ידנית מאומתת, trust הכי גבוה). נוסף **בית חב״ד שוהם** (הדקל 30, 054-549-6372). GovMap נפל זמנית → נוסף fallback geocoder (Nominatim, **רק ל-manual**, רחוב+מספר). `connect-live` חוזק עם שמירת-מרחק (60מ׳) למניעת כפילויות בריצה חוזרת. סה"כ chabad_house **10 → 11**, 0 מחיקות.
- **2026-06-23** — **בתי חב״ד — מיזוג Phase 3:** הורץ `connect-live.ts` (גדור, additive): **+10 בתי חב״ד** → סה"כ מקומות 5461 → **5471** (synagogue 4419 / mikveh 696 / restaurant 346 / chabad_house 10). 0 מחיקות, קטגוריות קיימות ללא שינוי. גיבוי: `places.osm.pre-chabad.backup.json`. אומת ב-bundle (chabad_house ×17). ⚠️ לא הורץ rebuildAppDataset.
- **2026-06-23** — **בתי חב״ד — מחקר מעמיק + Phase 3 (importer dry-run):** מחקר מרובה-מקורות (104 סוכנים) אישר: אין מקור פתוח שמגיע ל-90% עם קואורדינטות; chabad.org Locator חסום ב-ToS; הדרך ל-90% = הסכם עם צעירי אגודת חב״ד. נבנה importer חוקי `importers/chabad/` (OSM ODbL + data.gov.il עמותות `be5b7935-…` + Wikidata CC0 + GovMap geocode), dry-run בלבד. תוצאה: raw 123 (osm 32 / datagov 24 / wikidata 67) → 78 clusters → **10 חדשים write-ready**, 22 overlap-בתי-כנסת (מוחזקים), 8 לא-בתים, 38 ללא קואורדינטות; GovMap 0/21 (אין מספרי בית בעמותות). `connect-live.ts` מוכן אך **לא הורץ**. tsc ירוק.
- **2026-06-22** — **בתי חב״ד — Phase 2 (מודל + UI):** הוסף `PlaceType` חדש `chabad_house` (תווית "בית חב״ד", אייקון `home`, צבע סגול מעומעם `#6D4C9F`). שדות אופציונליים אדיטיביים: `website`, `contactPerson` (שליח/איש קשר), `services[]`. UI: קיצור בבית (שורה שנייה), כותרות מפה/רשימה, סיכת מפה סגולה, שורות פרטים (איש קשר/שירותים/אתר/מקור). i18n עודכן. **ללא ייבוא דאטה.** tsc ירוק (exit 0), bundle נקי (707 modules). additive בלבד, 0 שינוי בקטגוריות קיימות.
- **2026-06-19 → 06-22** — **ArcGIS REST — מקור עירוני (production):** נבנה **adapter גנרי אחד** (`importers/arcgis/`) לשכבות ESRI ArcGIS REST עירוניות פתוחות — עיר = שורת config (endpoint/wkid/idField/fieldMap), אפס שינוי בלוגיקה. `outSR=4326` תמיד (כולל הסטת‑הדאטום הישראלי ~77מ׳); native coords only; cityId לפי התאמת‑שם; שער ולידציה (0 מחיקות). הורחב מ‑POC חיפה ל‑**14 ערים** (ת"א/חיפה/אשדוד/אשקלון/ירושלים/נהריה/מודיעין/נוף הגליל/מודיעין עילית/חולון/הרצליה/לוד/אור עקיבא/קרית אונו) → **+1,857 בתי כנסת**. גם **GovMap** אומץ כ‑geocoder ישראלי אמין לרחובות (ITM→WGS84) למקוואות שחסרו קואורדינטות.
- **2026-06-24** — **בתי חב״ד — הרחבה ל‑74:** הורצו אצוות נוספות (נגב → צפון → connect סופי) דרך ה‑importer הרב‑מקורי החוקי (OSM ODbL + data.gov.il עמותות + Wikidata CC0 + manual, geocode GovMap). chabad_house **10 → 74** ב‑**44 ערים**; סה"כ מקומות **5,471 → 5,535** (479 ערים). connect סופי: +13 חדשים, 13 כפילויות דולגו, 0 מחיקות. גיבויים: `places.osm.json.bak-negev-*` / `bak-north-*` + גיבוי connect. הקטגוריות הקיימות (בתי כנסת/מקוואות/מסעדות) ללא שינוי. ⚠️ לא הורץ rebuildAppDataset.

- **2026-06-25** — **בתי חב״ד — הרחבה ל‑139:** אצווה נוספת (גליל + בתי חב״ד ממקורות נוספים). chabad_house **74 → 139**; סה"כ מקומות **5,535 → 5,600**.
- **2026-06-30 → 07-05** — **קברי צדיקים — קטגוריה חדשה (	zaddik_grave):** נבנה importer מלא ב-importers/tzaddik-graves/ (fetch-osm.ts + fetch-wikidata.ts + dedup.ts + validate.ts + seeds.ts + importer.ts). מקורות: OSM Overpass + Wikidata SPARQL. **29 קברי צדיקים חוברו** (write 05.07); backup: places.osm.pre-tzaddik.backup.json; cities.osm.json עודכן. PlaceType חדש עם אייקון ייעודי + צבע. סה"כ מקומות → **5,629** (6 קטגוריות).
- **2026-07-07** — **UX — אונבורדינג + מסכים חדשים:**
  - SplashScreen — לוגו עם אנימציית bounce + fade, מנווט אוטומטית לפי @karov/auth.
  - LoginScreen — מסך התחברות/הרשמה ראשוני.
  - LocationPermissionScreen — מסך בקשת הרשאת מיקום (slide_from_left animation).
  - ZmanimScreen — זמני היום (הלכה).
  - AddPlaceScreen — הוספת מקום חדש (modal).
  - Flow: Splash → Login → LocationPermission → Tabs.
- **2026-07-08** — **UI/UX — שדרוג גדול:**
  - AppMenu — תפריט bottom sheet: צור קשר / תרומה / שיתוף / אודות / שפה. נגיש מאייקון ☰ בבית.
  - AboutModal — מודאל "אודות קרוב".
  - LanguagePicker / LanguageContext — ריבוי שפות (עברית / אנגלית); כפתור גלובוס בכותרת הבית.
  - HomeScreen — שדרוג header: שורת top-bar (גלובוס | בס"ד + ☰), ברכה לפי יום השבוע, שם משתמש אישי, כרטיס CTA מוגבה עם subtitle, גריד קיצורים 3+2, קטגוריית קברי צדיקים.
  - ParashaDetailScreen + MapDetailScreen + CommunityScreen + BrachotScreen — מסכים נוספים.
  - KashruyotFilterScreen — סינון כשרות ייעודי לפני כניסה לרשימת מסעדות.
  - PlaceCard + PlaceDetailScreen — עדכוני עיצוב.
  - RootNavigator — הוספת כל המסכים החדשים לסטאק.
