# AGENTS.md — הנחיות לעבודה על הריפו הזה

## Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.
This project is on **SDK 57** (React Native 0.86). Do not rely on remembered APIs — check the versioned page first.

---

## הפקודה שחייבת לעבור

```bash
npm run verify      # typecheck + lint + test + data:validate
```

אל תסיים משימה בלי שהיא עוברת. זה בדיוק מה שה‑CI מריץ (`.github/workflows/ci.yml`).

> ### ⚠️ ירוק בעץ העבודה ≠ הקומיט תקין
>
> **Passing checks against the working tree is not sufficient evidence that a commit is valid.**
>
> כמה סשנים חולקים תיקיית עבודה אחת. שינוי לא‑מקומט או קובץ untracked של סשן אחר **מספק תלות בשקט**
> ו‑`npm run verify` עובר על מצב שלא קיים ב‑git. זה כבר קרה פעמיים בריפו הזה:
> `876a562` ייבא `isFoodType` בלי להוסיף את ה‑export (הסימבול חי ב‑`catalog.ts` שהיה untracked), ו‑`78209ae`
> קרא `place.certifierId` בלי שהשדה הוצהר בקומיט. שניהם עברו verify מקומי. שניהם נכשלו ב‑clone נקי.
>
> **לפני שמדווחים על קומיט משמעותי כמאומת — חובה בדיקת self‑containment ממצב נקי:**
>
> ```bash
> git worktree add --detach /tmp/headcheck HEAD
> # junction/symlink של node_modules לתוך ה‑worktree, ואז:
> cd /tmp/headcheck && npm run verify
> git worktree remove --force /tmp/headcheck
> ```
>
> `git show HEAD:<file>` הוא הכלי לבדיקה נקודתית — **עותק העבודה ישקר לך.**
> כל סימבול שקובץ מקומט מייבא חייב להיות בקומיט עצמו או כבר ב‑HEAD.

> ### ⚠️ טסט שאף אחד לא מריץ הוא קובץ, לא מנגנון
>
> `jest.config.js` מגדיר `roots: ['<rootDir>/src']` ו‑`testMatch` שמכסה רק `.[jt]s?(x)`.
> **כל טסט מחוץ ל‑`src/`, וכל טסט `.mjs`, לא רץ** — לא ב‑`npm test`, לא ב‑`verify`, לא ב‑CI.
> זה קרה שלוש פעמים: שלושה קבצי guard‑test עברו review, עברו ידנית, ולא רצו אף פעם אוטומטית.
>
> **לפני שמסתמכים על טסט — הוכיחו שהוא רץ:** `npx jest --listTests` חייב להחזיר אותו, או שהוא
> מחווט דרך `test:scripts` ב‑`package.json` **וגם** ב‑`ci.yml`. ספירת הסוויטות שלא זזה אחרי
> שהוספת קובץ טסט היא הסימן.
>
> **וכל guard חייב להיות מופעל, לא נקרא:** בצע את ההפרה, ראה אדום, החזר. guard שאיש לא ראה נכשל
> הוא קובץ עם שם של guard.

> ### ⚠️ אל תשנה את `src/data/generated/` בעץ המשותף כדי לבדוק משהו
>
> כמה סשנים חולקים checkout אחד. Ctrl+C, קריסה, או `git add` של סשן אחר בחלון שבין השינוי
> לשחזור — והתוצאה היא קורפציה מקומטת של הדאטהסט שנראית בדיוק כמו עריכה מכוונת.
> השחזור יכול להיות תקין לחלוטין וזה עדיין לא הנקודה: **חלון החשיפה קיים, על הנכס היחיד
> שהפרויקט הזה קיים כדי להגן עליו.**
>
> טסט שדורש מוטציה של `src/data/generated/` רץ ב‑worktree מנותק, לעולם לא ב‑checkout המשותף:
>
> ```bash
> git worktree add --detach /tmp/fixture HEAD
> # ...מוטציה, בדיקה, אימות...
> git worktree remove --force /tmp/fixture
> ```
>
> עולה כשתי שניות. ג'אנקשן של `node_modules` מוסר עם `cmd /c rmdir` — `Remove-Item -Recurse`
> עלול ללכת דרך הג'אנקשן ולמחוק את היעד האמיתי.
>
> #### ⛔ אבל worktree הוא הצגה מול סקריפט עם נתיב מוחלט
>
> **13 סקריפטים כותבים `C:\Users\User\Desktop\claude plane\kosher-app\...` ישירות. 11 מהם נמצאים ברשימת
> ההחרגה הקפואה** — כלומר דווקא אלה שנחשבים "בטוחים". הם לא נפתרים מהמיקום שלהם; הם יכתבו לעץ המשותף
> לא משנה מאיפה הרצת אותם. worktree לא מגן עליך מהם בכלל.
>
> **לפני שמריצים סקריפט לא מוכר — גם ב‑worktree — חובה לגרפ אותו לנתיב מוחלט.** יש נתיב מוחלט? הוא
> יכתוב לעץ המשותף. זה קרה: ריצה "מבודדת" של 7 סקריפטים החזירה 11 שורות כפולות ושחזרה 3 רשומות
> קיימות בעץ האמיתי, כולל `kosherType` שירד מ‑`badatz_beit_yosef` ל‑`kosher`.
>
> ואם אתה בונה את הכלי בעצמך: `grep` עם escaping ידני של `\` נכשל כאן פעמיים ברצף והחזיר "6" ואז מספר
> שלא כלל סקריפטים שנצפו כותבים בפועל. **תשתמש בכלי חיפוש אמיתי, לא ב‑shell escaping מאולתר** — מכשיר
> שבור נותן ביטחון שקרי, וזה §17 שוב.

> ### ⚠️ "כבר רץ פעם אחת" הוא לא ניתוח סיכון — והנזק האמיתי שקט
>
> רשימת ההחרגה מחריגה עשרות סקריפטים בנימוק "already executed once; no automated trigger to re-run".
> זו טענה על **האם** ירוץ, לא על **מה יקרה אם כן**. ומה שקורה מחולק לשניים:
>
> · **רועש** — הוספה כפולה. `data:validate` נכשל HARD על duplicate ids. אף אחד לא יכול לקמט את זה.
> · **שקט** — הפעלה חוזרת מחילה מחדש payload קפוא מיולי מעל שמונה חודשים של תיקונים מאוחרים:
>   שעות פתיחה בעברית חוזרות לתחביר OSM גולמי, URL של סניף חוזר לדומיין גנרי, `lastVerifiedAt` **אחורה**,
>   וטענת כשרות שיורדת מבד"ץ מפורש ל‑`kosher` סתמי. **כל אלה עוברים `data:validate` נקי. שום דבר לא תופס.**
>
> `lastVerifiedAt` שזז אחורה הוא החתימה. אם ראית אותה — סקריפט one‑shot רץ מחדש.

> ### ⚠️ ספירה נכונה על קובץ פסול היא עדיין כישלון
>
> **כל ריצה שמייצרת דאטהסט חייבת להסתיים ב‑`data:validate` על הפלט עצמו — לא בהשוואת מונים.**
>
> `import-food.mjs` ניבא 7471→7540, הניבוי נגזר ביד מראש, והוא התאים בדיוק. הפלט עדיין נכשל בוולידטור
> עם 37 מזהים כפולים, כי `nextIdx` נגזר מ‑**count** ולא מ‑**max** ובסדרה היו 43 חורים. הניבוי ענה על
> "כמה שורות". השאלה הייתה "האם השורות תקינות".
>
> זה הפרצוף הרביעי של אותה תקלה (ראה `docs/KASHRUT_FACTS.md` §17): טסט שאיש לא מריץ · fixture שלא
> מתאים לכלום · טסט שמעתיק את הלוגיקה במקום לייבא אותה · ניבוי שמתאים בנפח בזמן שהפלט פסול.
> בכל ארבעתם **הצורה של הבדיקה נשארה שלמה בזמן שהנושא שלה התרוקן** — ושום דבר לא זורק שגיאה.
>
> **הנגד‑אמצעי שמכליל, וזה מה שכדאי לזכור:** שאל *מה היה צריך להיות נכון כדי שהבדיקה הזאת תעבור בזמן
> שהדבר שהיא שומרת עליו שבור* — ואז לך תבדוק **את זה**. אף אחד מהארבעה לא נמצא בקריאת הבדיקה עצמה.

| בדיקה | פקודה | מה נכשל בה |
|---|---|---|
| טיפוסים | `npm run typecheck` | `tsc --strict`. חייב להיות נקי — הוא נקי היום |
| lint | `npm run lint` | **0 errors**. warnings = backlog ידוע, ראה למטה |
| טסטים | `npm test` | 233 טסטים (המספר גדל — הבדוק את הפלט בפועל, לא את המספר כאן) |
| דאטה | `npm run data:validate` | שלמות `places.osm.json` |

---

## מודל העבודה: Architect / Implementer / Reviewer

**זה לא הצעה. זה מודל התפעול של הפרויקט, והוא בתוקף גם בסשנים עתידיים.**
אם אתה סשן חדש בריפו הזה — קרא את זה לפני שאתה מתחיל לעבוד, וברר איזה תפקיד אתה ממלא.

| תפקיד | אחריות |
|---|---|
| **Architect** | הבעלים הטכני, המתזמר, ו**הממשק ההנדסי היחיד של בעל הפרויקט**. מפרק עבודה, מאציל, מעריך תוצאות, מכריע במחלוקות טכניות, שומר על עקביות ארכיטקטונית, וקובע מתי עבודה באמת הושלמה. |
| **Implementer** | מבצע את העבודה שה‑Architect הגדיר, לפי הארכיטקטורה והאילוצים שנקבעו. אם המימוש חושף בעיה ארכיטקטונית עמוקה או סותר הנחה קיימת — **מדווח ל‑Architect**, לא משנה כיוון פרויקט באופן עצמאי. |
| **Reviewer** | שכבת אימות **עצמאית**. מאתגר הנחות, נכונות מימוש, ארכיטקטורה, ראיות, רגרסיות, מקרי קצה, ומתודולוגיית האימות עצמה. **לא מאשר את דוח ה‑Implementer — מאמת אותו מחדש.** |

הלולאה: **Architect → תכנון → Implementer → Reviewer → תיקון → re‑review → קבלה ע"י Architect.**
היא רצה **פנימית**. בעל הפרויקט לא מתזמר אותה.

### תקשורת פנימית: עומק מלא, בלי קיצורים

בין הסוכנים — **אל תקצר על חשבון נכונות.** שמרו ממצאים, ראיות, הנחות, החלטות מימוש,
**אלטרנטיבות שנדחו ולמה**, מערכות מושפעות, טסטים, מקרי קצה, כשלים, התנגדויות של ה‑Reviewer,
השלכות ארכיטקטוניות, ובעיות פתוחות. המטרה: אפס אובדן מידע ואפס גילוי‑מחדש חוזר.

### תקשורת החוצה: דחוסה

בעל הפרויקט **לא** מקבל את התמליל ההנדסי — לא פקודות, לא קבצים שנבדקו, לא היפותזות ביניים,
לא לוגים, לא הערות Reviewer, לא מחזורי תיקון, לא שיחות בין סוכנים. ה‑Architect צורך את זה ודוחס.

דיווח שוטף = **Result · Important findings · Verification · Remaining issues · Next step** בלבד.
הסלמה = **Decision needed · Why it matters · Viable options · Architect recommendation** בלבד.
מסלימים רק החלטת בעלים אמיתית: tradeoff ארכיטקטוני, החלטה מוצרית, פעולה הרסנית/בלתי הפיכה,
דרישות סותרות, שינוי סקופ מהותי, אי‑ודאות שראיות לא פותרות, או סיכון שצריך לקבל במודע.
**אין לבקש אישור בין צעדים הנדסיים שגרתיים.**

### ידע קיים

`docs/KASHRUT_FACTS.md` הוא מרשם העובדות המאומתות. **קרא לפני חקירה מהותית.** מה שנקבע שם
נחשב ידע פרויקט — אל תגזור מחדש אלא אם יש ראיה חדשה לפתוח ערך ספציפי, ואז תקן את הקובץ.
ממצא חדש ועמיד — הוסף. הקובץ הוא המנגנון נגד גילוי‑מחדש, לא הערות פרטיות של מישהו.

### הגדרת "גמור"

קוד שנכתב ≠ עבודה שהושלמה. גמור =
**הובן → תוכנן → מומש → נבדק עצמאית → תוקן → אומת מול המצב המקומט → תועד.**

---

## גבולות ארכיטקטורה (נאכפים ב-ESLint, לא בכוונות טובות)

1. **הדאטה נגיש רק דרך `placesRepository`.**
   אסור `import` מ‑`src/data/generated/*` או מ‑`src/data/seed/*` מחוץ ל‑`src/data/`. ה‑seed הוא דמו פיקטיבי — אם הוא דולף לקוד ייצור, המשתמש רואה מקומות שלא קיימים.

2. **אין צבע גולמי מחוץ ל‑`src/theme/`.**
   `'#1E7A46'` בקומפוננטה = warning. הוסף ל‑`src/theme/colors.ts` וייבא מ‑`../theme`. בלי זה אין rebrand ואין dark mode.

3. **אין מחרוזת עברית קשיחה** ב‑`src/screens/`, `src/components/`, `src/navigation/`.
   הכל דרך `src/i18n/*.ts` ו‑`useLanguage()`. יש 5 שפות; מחרוזת קשיחה שוברת 4 מהן.

כללים 2–3 הם `warn` היום כי יש backlog ידוע (~900 מופעים, ראה `docs/ARCHITECTURE_REVIEW.md` §P1-1 ו‑§P1-2). **אל תוסיף מופעים חדשים.** כשספרייה מנוקה — העלה אותה ל‑`error` ב‑`eslint.config.js`.

---

## קבצים שנוצרים אוטומטית — לא לערוך ביד

| נתיב | נבנה ע"י |
|---|---|
| `src/data/generated/*.json` | `importers/` + `scripts/` |
| `dist/` | `expo export` |
| `scripts/data-quality-baseline.json` | `npm run data:baseline` |
| `data-backups/` | תמונות מצב היסטוריות — מחוץ ל‑git, לא לגעת |

---

## כללי הדאטה — אלה לא המלצות

- **Additive‑only. אפס מחיקות.** רשומה לא נמחקת אף פעם. אם היא שגויה — מתקנים אותה.
- **Provenance לכל רשומה.** `source` + `sourceUrl` + `lastVerifiedAt` ככל שאפשר.
- **מסעדה בלי כשרות לא נכנסת.** אין תעודה → אין רשומה.
- **`location` הוא תמיד `{ latitude, longitude }`.** הצורה `{ lat, lng }` נמחקת בשקט ע"י `sanitizePlace` והמקום נעלם מהאפליקציה בלי שגיאה. הוולידטור תופס את זה; `scripts/fix-location-shape.mjs` מתקן.
- **גיבוי לפני כל merge**, ואז `npm run data:validate`.

מקורות תוכן תורני: **רק Sefaria ו‑Chabad.org**. אסור לייחס לרש"י/רמב"ם/רד"ק בלי אימות מול המקור. זו תורה — טעות כאן היא לא באג.

---

## מה כן ומה לא בסקופ

- **`docs/ARCHITECTURE_REVIEW.md` הוא מפת הדרכים.** כל ממצא ממוספר (P0‑1, P1‑3, …). כשאתה מתקן — עדכן את הסטטוס שם.
- שינוי תוכן יומי = תוכן בלבד. בלי מסכי הגדרות ובלי העדפות משתמש, אלא אם התבקש.
- **פילטר הכשרות נשאר פשוט** (רבנות/בד"ץ ברמת קבוצה). המידע המפורט חי בכרטיס המקום, לא בפילטר.

---

## מלכודות ידועות

- **`window.__karovLoc`** — global על `window` ב‑`LocationContext`. לא להרחיב את השימוש; מיועד להסרה (P1‑4).
- **`src/theme/fonts.ts` עושה monkey‑patch ל‑`Text.render`** כדי למפות `fontWeight` למשפחת Heebo. שביר מול שדרוגי RN — אם טקסט מאבד משקל אחרי שדרוג, זה המקום.
- **23 סקריפטים ב‑`scripts/` מכילים נתיב מוחלט** (`C:\Users\...`). הם לא ירוצו ב‑CI ולא אצל אף אחד אחר.
- **אימות מסלול iOS אסור בדפדפן המובנה** — הוא מזדהה כאנדרואיד. לסמלץ את ה‑API החסר, לא לקבל תשובה "תקינה".
