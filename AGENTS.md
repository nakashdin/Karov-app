# AGENTS.md — הנחיות לעבודה על הריפו הזה

## Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.
This project is on **SDK 57** (React Native 0.86). Do not rely on remembered APIs — check the versioned page first.

---

## הפקודה שחייבת לעבור

```bash
npm run verify      # typecheck + lint + test + data:validate
```

אל תסיים משימה בלי שהיא עוברת. זה בדיוק מה שה‑CI מריץ (`.github/workflows/ci.yml`).

> ### ⚠️ ירוק בעץ העבודה ≠ הקומיט תקין
>
> **Passing checks against the working tree is not sufficient evidence that a commit is valid.**
>
> כמה סשנים חולקים תיקיית עבודה אחת. שינוי לא‑מקומט או קובץ untracked של סשן אחר **מספק תלות בשקט**
> ו‑`npm run verify` עובר על מצב שלא קיים ב‑git. זה כבר קרה פעמיים בריפו הזה:
> `876a562` ייבא `isFoodType` בלי להוסיף את ה‑export (הסימבול חי ב‑`catalog.ts` שהיה untracked), ו‑`78209ae`
> קרא `place.certifierId` בלי שהשדה הוצהר בקומיט. שניהם עברו verify מקומי. שניהם נכשלו ב‑clone נקי.
>
> **לפני שמדווחים על קומיט משמעותי כמאומת — חובה בדיקת self‑containment ממצב נקי, מהקומיט עצמו:**
>
> ```bash
> git worktree add --detach /tmp/headcheck <HASH>
> # junction/symlink של node_modules לתוך ה‑worktree, ואז:
> cd /tmp/headcheck && npm run verify
> cmd /c rmdir <path-to-node_modules-junction>   # להסיר את הג'אנקשן קודם — לא Remove-Item -Recurse
> git worktree remove --force /tmp/headcheck
> ```
>
> `<HASH>` הוא **הקומיט הנבדק עצמו** — לא `HEAD`, לא commit ההורה שלו, לא "הכי קרוב שנוח". הבדיקה
> שואלת "האם הקומיט הזה, כפי שהוא, מכיל את כל מה שהוא צריך" — ולא "האם הקבצים האלה עובדים ביחד
> כשמניחים אותם זה לצד זה". שני הניסוחים נשמעים זהים בדיווח חופשי ("הרצתי verify ב‑worktree וזה
> עבר") אבל הם טענות שונות, ורק הראשונה היא מה שהכלל הזה קיים בשבילו.
>
> **דיווח על הבדיקה חייב לכלול את ה‑hash שהועבר בפועל ל‑`worktree add`.** דיווח שאומר "worktree נקי,
> verify עבר" בלי hash הוא לא ניתן לשחזור על ידי מישהו שלא היה שם כשזה רץ — ואי אפשר להבדיל אותו
> מדיווח על overlay שגוי. נמצא בפועל, 2026‑08‑27 (`docs/KASHRUT_FACTS.md` §30g): סשן שרשם את הכלל
> הזה במפורש חזר, בפסקה הבאה באותה הודעה, ועשה `git worktree add --detach <path> f087a4e` כדי לבדוק
> קומיט `6790e2b` — checkout של ה‑הורה, overlay של הקבצים ששונו מעליו, בדיוק הדפוס שהכלל נועד למנוע.
> לא היה נזק בפועל (שינוי תגובות בלבד, בלי סימבול שיכול ללכת לאיבוד) והדיווח היה מדויק מספיק כדי
> שהטעות תיתפס — אבל **המרחק בין לכתוב כלל ובין ליישם אותו יכול להיות אפס.** אי אפשר לסמוך על
> "לזכור את הכלל" בפעם הבאה. הפרוצדורה הזאת קיימת כדי ש‑overlay על קומיט שאינו הנבדק יהיה הדרך
> שצריך *לנמק במפורש* לבחור בה, לא ברירת המחדל השקטה.
>
> `git show HEAD:<file>` הוא הכלי לבדיקה נקודתית — **עותק העבודה ישקר לך.**
> כל סימבול שקובץ מקומט מייבא חייב להיות בקומיט עצמו או כבר ב‑HEAD.

> ### ⚠️ טסט שאף אחד לא מריץ הוא קובץ, לא מנגנון
>
> `jest.config.js` מגדיר `roots: ['<rootDir>/src']` ו‑`testMatch` שמכסה רק `.[jt]s?(x)`.
> **כל טסט מחוץ ל‑`src/`, וכל טסט `.mjs`, לא רץ** — לא ב‑`npm test`, לא ב‑`verify`, לא ב‑CI.
> זה קרה שלוש פעמים: שלושה קבצי guard‑test עברו review, עברו ידנית, ולא רצו אף פעם אוטומטית.
>
> **לפני שמסתמכים על טסט — הוכיחו שהוא רץ:** `npx jest --listTests` חייב להחזיר אותו, או שהוא
> מחווט דרך `test:scripts` ב‑`package.json` **וגם** ב‑`ci.yml`. ספירת הסוויטות שלא זזה אחרי
> שהוספת קובץ טסט היא הסימן.
>
> **וכל guard חייב להיות מופעל, לא נקרא:** בצע את ההפרה, ראה אדום, החזר. guard שאיש לא ראה נכשל
> הוא קובץ עם שם של guard.

> ### ⚠️ אל תשנה את `src/data/generated/` בעץ המשותף כדי לבדוק משהו
>
> כמה סשנים חולקים checkout אחד. Ctrl+C, קריסה, או `git add` של סשן אחר בחלון שבין השינוי
> לשחזור — והתוצאה היא קורפציה מקומטת של הדאטהסט שנראית בדיוק כמו עריכה מכוונת.
> השחזור יכול להיות תקין לחלוטין וזה עדיין לא הנקודה: **חלון החשיפה קיים, על הנכס היחיד
> שהפרויקט הזה קיים כדי להגן עליו.**
>
> טסט שדורש מוטציה של `src/data/generated/` רץ ב‑worktree מנותק, לעולם לא ב‑checkout המשותף:
>
> ```bash
> git worktree add --detach /tmp/fixture HEAD
> # ...מוטציה, בדיקה, אימות...
> git worktree remove --force /tmp/fixture
> ```
>
> עולה כשתי שניות. ג'אנקשן של `node_modules` מוסר עם `cmd /c rmdir` — `Remove-Item -Recurse`
> עלול ללכת דרך הג'אנקשן ולמחוק את היעד האמיתי.
>
> #### ⛔ אבל worktree הוא הצגה מול סקריפט עם נתיב מוחלט
>
> **הסכנה היא "סקריפט כותב לדאטהסט המשותף דרך נתיב מוחלט" — לא "סקריפט כשרות".** אלה שתי אוכלוסיות
> שונות שבמקרה חופפות בחלקן. גרסה קודמת של האזהרה הזאת תיארה "13 סקריפטים, 11 מהם ברשימת ההחרגה
> הקפואה של כשרות" — וזו הייתה טעות תיחום, לא רק ניסוח: מי שגזר את רשימת העבודה מרשימת ההחרגה של
> `kashrut-write-completeness.test.mjs` קיבל בדיוק את ה‑13 שנוגעים בשדות כשרות, ופספס **6 סקריפטים
> נוספים** שכותבים ישירות ל‑`places.osm.json`/`restaurants.osm.json` ונעדרים מרשימת הכשרות **מהסיבה
> הנכונה** — הם לא נוגעים באף שדה כשרות. רשימה היא proxy לשאלה שהיא נבנתה בשבילה, ולא לשום שאלה אחרת.
> אותה טעות בדיוק גרמה גם לסבב ה‑re‑run האדוורסרי לבחור 7 סקריפטים מאותה רשימה ולהינכות מארבעה מהם.
>
> **המצב המתוקן, מאומת בסריקת קבצים מלאה על כל הריפו (לא grep עם escaping):** 10 סקריפטים מכילים
> נתיב מוחלט `C:\Users\...` או `C:/Users/...` (שני סגנונות ה‑slash כאחד — סקריפטים שונים משתמשים
> בשניהם). **6 כותבים בפועל ל‑places.osm.json/restaurants.osm.json**, ותוקנו: `cleanup-greg-duplicates`,
> `cleanup-manual-dups`, `dedup-all-food`, `fix-pizza-shemesh-dups`, `fix-pizzahut-missing`,
> `sync-greg-places`. **כל ששת אלה מוחקים רשומות** (`data.filter(...)` שמצטמצם ואז נכתב) — זה נבדק
> בקריאה ישירה של כל קובץ, לא ברגקס: ניחוש מוקדם קבע "5 מתוך 6", ניחוש אחר קבע "2", שניהם החליפו קריאה
> בהיוריסטיקה. **מה שהתברר על ה‑4 הנותרים תוקן פעמיים — פעם בהבנה, פעם בקוד.** התיאור הראשון
> ("3 מצביעים ל‑scratchpad של סשן שכבר לא קיים") היה שגוי: תיקיית הסשן `3d5b0d60-...` **קיימת**
> (מתוארכת, עם `scratchpad/` ו‑`tasks/` שלמים) — רק שני קבצי ה‑JSON הספציפיים בתוכה נעלמו. בטיחות
> לפי **העדר קובץ קלט**, לא לפי העדר סשן — הבדל שמשנה את כל הטיעון: `import-roladin.mjs`,
> `import-pizzahut.mjs` ו‑`fix-roladin-final.mjs` כתבו את נתיב הדאטהסט **יחסית ל‑CWD** —
> `'src/data/generated/places.osm.json'` בלי שום `fileURLToPath` — כלומר גם worktree לא היה מגן
> עליהם: מי שמריץ אותם מהשורש של העץ המשותף (גם לנתיב סקריפט בתוך worktree) כותב לעץ המשותף. מה
> שעצר אותם היה קריסה על קריאת הקלט **לפני** שהם מגיעים לכתיבה — מנגנון בטיחות מקרי, לא מתוכנן.
> **תוקנו לאותה תבנית כמו כל השאר** (`fileURLToPath(import.meta.url)` → root → נתיב יחסי לכתיבה
> בלבד; נתיב הקלט המוחלט ל‑scratchpad לא נגע). `build-data-sources-doc.mjs` נשאר כפי שהוא —
> הנתיב המוחלט שם הוא `require()` של חבילת npm גלובלית, לא נוגע בדאטהסט כלל, קורא אותו בנתיב יחסי
> תקין וכותב רק `.docx`. אין תועלת בתיקון קוד שממילא לא יכול להגיע לדאטהסט.
>
> ה‑13 המקוריים תוקנו ראשונים (קומיט `f0a068b`), ה‑6 שכותבים ישירות אחריהם (`c35e116`), ושלושת אלה
> אחרונים — אותה תבנית בדיוק בכל שלושת הסבבים. **סה"כ 22 סקריפטים תוקנו. האוכלוסייה אינה סגורה** —
> היא כל סקריפט שכותב את הדאטהסט המשותף בנתיב מוחלט (או בנתיב יחסי ל‑CWD, שזה אותו כשל בלבוש אחר),
> ואת זה בודקים בסריקה על כל הריפו, לא ברשימת עבודה ממוקדת נושא ולא במספר קבוע.
>
> **לפני שמריצים סקריפט לא מוכר — גם ב‑worktree — חובה לסרוק אותו לנתיב מוחלט.** יש נתיב מוחלט? הוא
> יכתוב לעץ המשותף. זה קרה: ריצה "מבודדת" של 7 סקריפטים החזירה 11 שורות כפולות ושחזרה 3 רשומות
> קיימות בעץ האמיתי, כולל `kosherType` שירד מ‑`badatz_beit_yosef` ל‑`kosher`.
>
> **איך סורקים נכון:** סקריפט Node שקורא כל קובץ `.mjs/.js/.ts` בריפו (לא רק בתיקייה שנראית רלוונטית)
> ומחפש את המחרוזת המילולית פעמיים — עם `\` ועם `/`. לא `grep` עם escaping ידני של backslash.
>
> **המספר "שלוש פעמים בין שלושה סוכנים" עצמו היה ספירה חסרה — בדיוק הנושא שהוא מתעד.** שלושה סוכנים,
> אבל **לפחות ארבעה אירועי כישלון נפרדים**: שני הכשלים של ה‑escaping הידני, ועוד — ל‑Reviewer היו
> **שני דפוסים שבורים שונים לגמרי** שהחזירו את אותה תשובה "6" במקרה, לא כישלון בודד. סוכן = לא אירוע.
>
> **הפנים החמישית של §17 (`docs/KASHRUT_FACTS.md`), ניסוח ה‑Reviewer — החדה מכל מה שנאמר היום:**
> "0 תוצאות" חשוד בדיוק כמו מספר לא‑אפס שגוי — ומעבר לזה, הוא התוצאה **היחידה שמסתירה את עצמה**.
> מספר לא‑אפס שגוי מזמין בדיקה ("למה זה כל כך גבוה/נמוך?"); אפס נראה **בו‑זמנית** כמו תשובה נקייה
> וכמו העדר עבודה — אין שום סימן חיצוני שמבדיל "סרקתי הכל ולא מצאתי" מ"הכלי שלי מכסה חצי מהמרחב
> ופספס הכל". זה קרה כאן: סריקת קבצים מלאה שחיפשה רק את סגנון ה‑`\` החזירה "0" על ריפו עם 6 קבצים
> אמיתיים בסגנון `/`. **התרופה הפרוצדורלית היא לא לבדוק את המחט שוב — לפתוח קובץ שאתה כבר בטוח
> שהוא hit, ולקרוא אותו.** בדיקת המכשיר מול positive ידוע היא הדרך היחידה לגלות מחט שמכסה חצי
> מהמרחב; קריאה חוזרת של אותה סריקה רק מאשרת את אותו כישלון בביטחון רב יותר.

> ### ⚠️ `git push` דוחף את הענף, לא את הקומיט שלך
>
> בעץ משותף הענף צובר קומיטים של סשנים אחרים, ו‑`git push` שולח את **כולם**. קרה בפועל, 2026‑08‑27:
> ה‑Reviewer דחף קומיט תיעוד (`6bc05f1`) ובכך שלח לרימוט קומיט של ה‑Implementer (`80f51dc`) שהוא לא קרא.
> לא היה נזק — הקומיט היה תקין ואומת בדיעבד — אבל **שער הסקירה נעקף בשקט: אף אחד לא ביצע פעולה**
> **שנראית כמו דילוג על סקירה.** זו לא תקלה חד‑פעמית אלא תכונה מבנית של checkout משותף, והיא חוזרת
> בכל push שמישהו עושה כשענף מכיל עבודה של סשן אחר.
>
> **לפני push — תסתכל מה אתה שולח בפועל:**
>
> ```bash
> git log --oneline origin/<branch>..HEAD
> ```
>
> `.githooks/pre-push` מדפיס את הרשימה הזאת אוטומטית בכל push, כדי שהמידע יופיע ברגע הפעולה ולא
> יהיה תלוי בזה שמישהו יזכור להריץ את הפקודה מראש.
>
> **⚠️ הוא מדפיס בלבד. הוא לא חוסם, והוא לא יכול.** אל תסיקו מקיומו שקומיטים זרים נמנעים — הם לא.
> שער אמיתי היה צריך להכריע "הקומיט הזה אינו שלך", ו‑git לא יכול לענות על זה כאן: **כל הסשנים**
> **מקמטים עם אותו author ואותו email**, ואין שום מטא‑דאטה בריפו שמבדילה ביניהם. (שמות המודל ב‑
> `Co-Authored-By` במקרה שונים היום. זה צירוף מקרים של איזה מודל רץ בכל סשן, לא זהות — והוא יפסיק
> להבחין בשקט ברגע ששני סשנים ירוצו על אותו מודל. אל תבנו בדיקה על זה; ראה `docs/KASHRUT_FACTS.md` §17c.)
>
> **הגבול הזה הוא החלטה מודעת ולא פער שנשכח** — נבחרה נראוּת על פני שער, כי שער שנשען על זהות שאי
> אפשר להוכיח נכשל בשקט, ושער שחוסם בטעות סשן אחר באמצע תיקון מזיק יותר מהסכנה שהוא מונע. הסיכון
> שנשאר, במפורש: אפשר לקרוא את הפלט ולהתעלם ממנו.

> ### ⚠️ "האם העבודה שלי נדחפה?" — `git ls-remote` לא עונה על זה
>
> הבדיקה הנכונה היחידה:
>
> ```bash
> git log --oneline origin/<branch>..HEAD     # ריק = נדחף
> ```
>
> **השוואת SHA לראש הרימוט אינה מספיקה.** קומיט יכול להיות ברימוט מבלי להיות ראש הענף — כהורה שלו.
> קרה בפועל, 2026‑08‑27: `80f51dc` לא היה שווה לראש הרימוט (`6bc05f1`) ו**כן** היה שם, כהורה שלו.
> דיווח שגוי "העבודה שלך מעולם לא נדחפה" כמעט נשלח ל‑Implementer על סמך ההשוואה הזאת, והשיטה הזאת
> הופצה קודם לכן לשני סשנים כהמלצה. **tip‑equality אינה reachability** — הן עונות על שאלות שונות,
> והן מתלכדות רק כשהקומיט שלך במקרה הוא האחרון בענף.

> ### ⚠️ "כבר רץ פעם אחת" הוא לא ניתוח סיכון — והנזק האמיתי שקט
>
> רשימת ההחרגה מחריגה עשרות סקריפטים בנימוק "already executed once; no automated trigger to re-run".
> זו טענה על **האם** ירוץ, לא על **מה יקרה אם כן**. ומה שקורה מחולק לשניים:
>
> · **רועש** — הוספה כפולה. `data:validate` נכשל HARD על duplicate ids. אף אחד לא יכול לקמט את זה.
> · **שקט** — הפעלה חוזרת מחילה מחדש payload קפוא מיולי מעל שמונה חודשים של תיקונים מאוחרים:
>   שעות פתיחה בעברית חוזרות לתחביר OSM גולמי, URL של סניף חוזר לדומיין גנרי, `lastVerifiedAt` **אחורה**,
>   וטענת כשרות שיורדת מבד"ץ מפורש ל‑`kosher` סתמי. **כל אלה עוברים `data:validate` נקי. שום דבר לא תופס.**
>
> `lastVerifiedAt` שזז אחורה הוא החתימה. אם ראית אותה — סקריפט one‑shot רץ מחדש.

> ### ⚠️ ספירה נכונה על קובץ פסול היא עדיין כישלון
>
> **כל ריצה שמייצרת דאטהסט חייבת להסתיים ב‑`data:validate` על הפלט עצמו — לא בהשוואת מונים.**
>
> `import-food.mjs` ניבא 7471→7540, הניבוי נגזר ביד מראש, והוא התאים בדיוק. הפלט עדיין נכשל בוולידטור
> עם 37 מזהים כפולים, כי `nextIdx` נגזר מ‑**count** ולא מ‑**max** ובסדרה היו 43 חורים. הניבוי ענה על
> "כמה שורות". השאלה הייתה "האם השורות תקינות".
>
> זה הפרצוף הרביעי של אותה תקלה (ראה `docs/KASHRUT_FACTS.md` §17): טסט שאיש לא מריץ · fixture שלא
> מתאים לכלום · טסט שמעתיק את הלוגיקה במקום לייבא אותה · ניבוי שמתאים בנפח בזמן שהפלט פסול.
> בכל ארבעתם **הצורה של הבדיקה נשארה שלמה בזמן שהנושא שלה התרוקן** — ושום דבר לא זורק שגיאה.
>
> **הנגד‑אמצעי שמכליל, וזה מה שכדאי לזכור:** שאל *מה היה צריך להיות נכון כדי שהבדיקה הזאת תעבור בזמן
> שהדבר שהיא שומרת עליו שבור* — ואז לך תבדוק **את זה**. אף אחד מהארבעה לא נמצא בקריאת הבדיקה עצמה.

| בדיקה | פקודה | מה נכשל בה |
|---|---|---|
| טיפוסים | `npm run typecheck` | `tsc --strict`. חייב להיות נקי — הוא נקי היום |
| lint | `npm run lint` | **0 errors**. warnings = backlog ידוע, ראה למטה |
| טסטים | `npm test` | 233 טסטים (המספר גדל — הבדוק את הפלט בפועל, לא את המספר כאן) |
| דאטה | `npm run data:validate` | שלמות `places.osm.json` |

---

## מודל העבודה: Architect / Implementer / Reviewer

**זה לא הצעה. זה מודל התפעול של הפרויקט, והוא בתוקף גם בסשנים עתידיים.**
אם אתה סשן חדש בריפו הזה — קרא את זה לפני שאתה מתחיל לעבוד, וברר איזה תפקיד אתה ממלא.

| תפקיד | אחריות |
|---|---|
| **Architect** | הבעלים הטכני, המתזמר, ו**הממשק ההנדסי היחיד של בעל הפרויקט**. מפרק עבודה, מאציל, מעריך תוצאות, מכריע במחלוקות טכניות, שומר על עקביות ארכיטקטונית, וקובע מתי עבודה באמת הושלמה. |
| **Implementer** | מבצע את העבודה שה‑Architect הגדיר, לפי הארכיטקטורה והאילוצים שנקבעו. אם המימוש חושף בעיה ארכיטקטונית עמוקה או סותר הנחה קיימת — **מדווח ל‑Architect**, לא משנה כיוון פרויקט באופן עצמאי. |
| **Reviewer** | שכבת אימות **עצמאית**. מאתגר הנחות, נכונות מימוש, ארכיטקטורה, ראיות, רגרסיות, מקרי קצה, ומתודולוגיית האימות עצמה. **לא מאשר את דוח ה‑Implementer — מאמת אותו מחדש.** |

הלולאה: **Architect → תכנון → Implementer → Reviewer → תיקון → re‑review → קבלה ע"י Architect.**
היא רצה **פנימית**. בעל הפרויקט לא מתזמר אותה.

### תקשורת פנימית: עומק מלא, בלי קיצורים

בין הסוכנים — **אל תקצר על חשבון נכונות.** שמרו ממצאים, ראיות, הנחות, החלטות מימוש,
**אלטרנטיבות שנדחו ולמה**, מערכות מושפעות, טסטים, מקרי קצה, כשלים, התנגדויות של ה‑Reviewer,
השלכות ארכיטקטוניות, ובעיות פתוחות. המטרה: אפס אובדן מידע ואפס גילוי‑מחדש חוזר.

### תקשורת החוצה: דחוסה

בעל הפרויקט **לא** מקבל את התמליל ההנדסי — לא פקודות, לא קבצים שנבדקו, לא היפותזות ביניים,
לא לוגים, לא הערות Reviewer, לא מחזורי תיקון, לא שיחות בין סוכנים. ה‑Architect צורך את זה ודוחס.

דיווח שוטף = **Result · Important findings · Verification · Remaining issues · Next step** בלבד.
הסלמה = **Decision needed · Why it matters · Viable options · Architect recommendation** בלבד.
מסלימים רק החלטת בעלים אמיתית: tradeoff ארכיטקטוני, החלטה מוצרית, פעולה הרסנית/בלתי הפיכה,
דרישות סותרות, שינוי סקופ מהותי, אי‑ודאות שראיות לא פותרות, או סיכון שצריך לקבל במודע.
**אין לבקש אישור בין צעדים הנדסיים שגרתיים.**

### ⚠️ ה‑Reviewer שעונה רק על השאלה שנשאלה מאשר עיצוב גרוע

**ביקורת אדוורסרית שמתוחמת ע"י מי שנבדק היא לא ביקורת.** זה קרה כאן, ונתפס רק במקרה:

ה‑Architect הציע קובץ ארכיון ל‑179 רשומות בלי ראיות כשרות, **וגם ניסח בעצמו את ההתנגדות** — "זה שוב
`restaurants.osm.json` בשם אחר" — וביקש מה‑Reviewer לתקוף אותה. ההתנגדות הזאת **נכשלת לגופה**: ארכיון יכול
להיות write‑once, מכוסה ratchet, ובלתי מסוגל להתפצל, כלומר שלוש מתוך חמש הסכנות של הקובץ המקורי נעדרות
מבנית. Reviewer שהיה עונה רק על מה שנשאל היה מאשר — וה‑Architect היה נשאר עם הארכיון **ומרגיש מאומת.**

ההתנגדות שהרגה את ההצעה הייתה אחרת לגמרי: **ארכיון אוכף אינרטיות דרך היעדר קוראים.** "האפליקציה לא קוראת
את זה" היא תכונה של גרף ה‑import של היום, לא אינvariant — שום דבר לא נכשל כשמישהו מוסיף קורא. כל מנגנון
שנבנה בפרויקט הזה החליף "אף אחד לא יעשה" ב‑"משהו בודק"; ההצעה הייתה "אף אחד לא יעשה".

**הכלל:** לא "האם ההתנגדות שהוצגה מחזיקה" אלא **"מה היה צריך להיות נכון כדי שזה יהיה בטוח"** — ואז לבדוק
את זה. אותה מטא‑כללה של §17, מופנית להצעה במקום לבדיקה. מי שנבדק לא קובע את גבולות הביקורת.

**ומה שיצא מזה:** סירוב ה‑framing ייצר החלטה שלמה שאיש לא ידע שהיא קיימת — 20 מסעדות מוצגות באפליקציה
היום בלי שום מידע כשרות, לא "לא ידוע", **כלום**. היא לא נמצאה בחיפוש אחריה.

### ידע קיים

`docs/KASHRUT_FACTS.md` הוא מרשם העובדות המאומתות. **קרא לפני חקירה מהותית.** מה שנקבע שם
נחשב ידע פרויקט — אל תגזור מחדש אלא אם יש ראיה חדשה לפתוח ערך ספציפי, ואז תקן את הקובץ.
ממצא חדש ועמיד — הוסף. הקובץ הוא המנגנון נגד גילוי‑מחדש, לא הערות פרטיות של מישהו.

### הגדרת "גמור"

קוד שנכתב ≠ עבודה שהושלמה. גמור =
**הובן → תוכנן → מומש → נבדק עצמאית → תוקן → אומת מול המצב המקומט → תועד.**

---

## גבולות ארכיטקטורה (נאכפים ב-ESLint, לא בכוונות טובות)

1. **הדאטה נגיש רק דרך `placesRepository`.**
   אסור `import` מ‑`src/data/generated/*` או מ‑`src/data/seed/*` מחוץ ל‑`src/data/`. ה‑seed הוא דמו פיקטיבי — אם הוא דולף לקוד ייצור, המשתמש רואה מקומות שלא קיימים.

2. **אין צבע גולמי מחוץ ל‑`src/theme/`.**
   `'#1E7A46'` בקומפוננטה = warning. הוסף ל‑`src/theme/colors.ts` וייבא מ‑`../theme`. בלי זה אין rebrand ואין dark mode.

3. **אין מחרוזת עברית קשיחה** ב‑`src/screens/`, `src/components/`, `src/navigation/`.
   הכל דרך `src/i18n/*.ts` ו‑`useLanguage()`. יש 5 שפות; מחרוזת קשיחה שוברת 4 מהן.

כללים 2–3 הם `warn` היום כי יש backlog ידוע (~900 מופעים, ראה `docs/ARCHITECTURE_REVIEW.md` §P1-1 ו‑§P1-2). **אל תוסיף מופעים חדשים.** כשספרייה מנוקה — העלה אותה ל‑`error` ב‑`eslint.config.js`.

---

## קבצים שנוצרים אוטומטית — לא לערוך ביד

| נתיב | נבנה ע"י |
|---|---|
| `src/data/generated/*.json` | `importers/` + `scripts/` |
| `dist/` | `expo export` |
| `scripts/data-quality-baseline.json` | `npm run data:baseline` |
| `data-backups/` | תמונות מצב היסטוריות — מחוץ ל‑git, לא לגעת |

---

## כללי הדאטה — אלה לא המלצות

- **Additive‑only. אפס מחיקות.** רשומה לא נמחקת אף פעם. אם היא שגויה — מתקנים אותה.
- **Provenance לכל רשומה.** `source` + `sourceUrl` + `lastVerifiedAt` ככל שאפשר.
- **מסעדה בלי כשרות לא נכנסת.** אין תעודה → אין רשומה.
- **`location` הוא תמיד `{ latitude, longitude }`.** הצורה `{ lat, lng }` נמחקת בשקט ע"י `sanitizePlace` והמקום נעלם מהאפליקציה בלי שגיאה. הוולידטור תופס את זה; `scripts/fix-location-shape.mjs` מתקן.
- **גיבוי לפני כל merge**, ואז `npm run data:validate`.

מקורות תוכן תורני: **רק Sefaria ו‑Chabad.org**. אסור לייחס לרש"י/רמב"ם/רד"ק בלי אימות מול המקור. זו תורה — טעות כאן היא לא באג.

---

## מה כן ומה לא בסקופ

- **`docs/ARCHITECTURE_REVIEW.md` הוא מפת הדרכים.** כל ממצא ממוספר (P0‑1, P1‑3, …). כשאתה מתקן — עדכן את הסטטוס שם.
- שינוי תוכן יומי = תוכן בלבד. בלי מסכי הגדרות ובלי העדפות משתמש, אלא אם התבקש.
- **פילטר הכשרות נשאר פשוט** (רבנות/בד"ץ ברמת קבוצה). המידע המפורט חי בכרטיס המקום, לא בפילטר.

---

## מלכודות ידועות

- **`window.__karovLoc`** — global על `window` ב‑`LocationContext`. לא להרחיב את השימוש; מיועד להסרה (P1‑4).
- **`src/theme/fonts.ts` עושה monkey‑patch ל‑`Text.render`** כדי למפות `fontWeight` למשפחת Heebo. שביר מול שדרוגי RN — אם טקסט מאבד משקל אחרי שדרוג, זה המקום.
- **23 סקריפטים ב‑`scripts/` מכילים נתיב מוחלט** (`C:\Users\...`). הם לא ירוצו ב‑CI ולא אצל אף אחד אחר.
- **אימות מסלול iOS אסור בדפדפן המובנה** — הוא מזדהה כאנדרואיד. לסמלץ את ה‑API החסר, לא לקבל תשובה "תקינה".
