{
  "generatedNote": "PHASE 9 — first-write apply SUMMARY. Plan only; nothing written or published.",
  "recordsToAdd": {
    "total": 110,
    "writeReadyNow": 61,
    "deferredPendingGeocode": 49
  },
  "schemaBlockers": [
    "B1: importers/shared/database.ts — APP_TYPES = ['synagogue','restaurant'] EXCLUDES 'mikveh', so rebuildAppDataset() drops every mikveh record.",
    "B2: Place.location is REQUIRED, but 49/110 records (all municipal/א.ש בינה) have NO native coordinates. They cannot become valid Place records until geocoded.",
    "B3: No mapper converts a unified NormalizedImportRecord → app Place for these council/municipal records. mapMikvahToPlace.ts only accepts the datagov GeocodedMikvah shape."
  ],
  "schemaNotes": [
    "B4: Place has no `extra` field, so mikveh-specific data (gender/category, balanit/attendant, sourceUrl, council provenance) would be DROPPED on write. Place.source enum is only osm|seed → council/municipal collapses to \"seed\".",
    "B5: CATEGORY_FILES['mikveh'] = 'mikvahs.datagov.json' — a misleading name for council/municipal records (and would collide if gov mikvahs are later published to the same file)."
  ],
  "schemaSupportVerdict": "TYPE-SYSTEM & UI READY for mikveh — no PlaceType change required.",
  "filesThatWouldBeModified": [
    "importers/shared/database.ts (edit-existing) — add 'mikveh' to APP_TYPES (line 26)",
    "importers/shared/mapUnifiedMikvahToPlace.ts (create-new) — NEW mapper: NormalizedImportRecord → Place",
    "src/data/generated/mikvahs.json (create-generated) — NEW generated category file holding the mikveh Place records (additive)",
    "src/data/generated/places.osm.json (regenerated) — REGENERATED by rebuildAppDataset() (the app dataset) — not hand-edited",
    "src/data/generated/cities.osm.json (regenerated) — REGENERATED by rebuildAppDataset() — adds mikveh cities",
    "src/types/place.ts (optional) — OPTIONAL: add `extra`/mikveh fields (only if preserving gender/balanit/sourceUrl)",
    "src/screens/HomeScreen.tsx (optional) — OPTIONAL: add a \"מקווה\" shortcut/entry point if not already present"
  ],
  "writeOrder": [
    "0. PRE: create backup src/data/generated/places.osm.pre-mikveh.backup.json (matches existing pre-step1/precouncils convention).",
    "1. WRITE 1 (ready now): map the 61 coordinate-bearing records → Place JSON via the new mapper.",
    "2. add 'mikveh' to APP_TYPES in database.ts; write the mikveh category file (src/data/generated/mikvahs.json).",
    "3. run rebuildAppDataset() → regenerates places.osm.json + cities.osm.json WITH the mikveh records.",
    "4. run post-write validation checks (below). If any fail → rollback.",
    "5. WRITE 2 (deferred): geocode the 49 coordless records (offline cached geocoder) → re-run dedup vs the now-published set → map resolved ones → repeat steps 2-4 additively.",
    "6. ONLY AFTER both writes validate: process the held enrich/manual/geocoding-needed items, then national aggregators."
  ],
  "rollbackPlan": [
    "Additive-only design: every new record has a fresh id and no existing place id is touched → rollback is removal, not repair.",
    "1. restore src/data/generated/places.osm.json from places.osm.pre-mikveh.backup.json (and cities.osm.json similarly).",
    "2. revert the database.ts APP_TYPES edit (git checkout importers/shared/database.ts).",
    "3. delete src/data/generated/mikvahs.json (the new category file).",
    "4. re-run rebuildAppDataset() to confirm the dataset matches the pre-write backup (diff record count + ids).",
    "5. git status / git diff to confirm no stray app-data changes remain."
  ],
  "postWriteValidationChecks": [
    "places.osm.json record count increased by EXACTLY 61 (write 1); no pre-existing id removed or modified (diff old vs new id sets).",
    "every new record: valid location (lat 29.3–33.5, lng 34.2–35.95), non-empty name, type===\"mikveh\", non-empty cityId.",
    "no id collision between new mikveh ids and existing place ids.",
    "cities.osm.json contains the new mikveh cities; counts re-sorted correctly.",
    "npx tsc -p importers/tsconfig.json --noEmit passes; app typecheck/build passes.",
    "app smoke test: mikveh pins render with the 'water' icon, the מקווה filter returns them, detail screen opens."
  ],
  "finalApprovalChecklist": [
    "Reviewer approved the 110-record package (Phase 8 review).",
    "Confirmed additive-only: the write adds 61 new ids and modifies/removes 0 existing places.",
    "Decided Tier-B handling: geocode-then-write the 49 coordless records as a SEPARATE second write (not in write 1).",
    "Approved the database.ts APP_TYPES change and the new mapper (B1, B3).",
    "Backup of places.osm.json created (step 0).",
    "Decided B4 (drop mikveh-specific fields vs extend Place) and B5 (category file name).",
    "Post-write validation checks defined and ready to run.",
    "EXPLICIT GO for the first write given by a human."
  ],
  "recordsByCityWriteReady": {
    "פתח תקווה": 12,
    "נתניה": 13,
    "באר שבע": 1,
    "ראש העין": 1,
    "מרום גליל": 19,
    "מרחבים": 7,
    "גבעת זאב": 2,
    "בית שאן": 1,
    "גני תקווה": 1,
    "קרית ארבע": 3,
    "שדרות": 1
  },
  "estimatedNationalTotal": {
    "afterWrite1": 667,
    "afterWrite2": 716,
    "note": "write 1 adds 61 (coordinate-bearing) → ~667; write 2 adds 49 after geocoding → ~716."
  },
  "nextStep": "Obtain explicit human GO, then execute write 1 (Tier A) per writeOrder. Do NOT proceed without sign-off on the approval checklist.",
  "dryRun": true,
  "liveDataTouched": false,
  "publishPerformed": false
}